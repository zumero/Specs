---
name: ASCollectionViewController
version: '0.1'
summary: A UICollectionViewController subclass that makes working with NSFetchedResultsController,
  UIMenuController and UIRefreshControl easier.
description: |2
                        A `UICollectionViewController` subclass that makes working with `NSFetchedResultsController`, `UIMenuController` and `UIRefreshControl` easier.

                        In particular it:

                        * Takes care of batching together any changes to your model to update the `collectionView`. This includes call backs if you need additional logic to respond to deletions/insertions/updates/moves.
                        * Enables long-tap menus on cells using `UIMenuItem` objects.
                        * Add a `UIRefreshControl` control.
homepage: https://github.com/nighthawk/ASCollectionViewController
license: FreeBSD
authors:
  Adrian Schoenig: adrian.schoenig@gmail.com
source:
  git: https://github.com/nighthawk/ASCollectionViewController.git
  tag: v0.1
platforms:
  ios: '6.0'
source_files: '*.{h,m}'
frameworks: CoreData
requires_arc: true
